import { TypeCheckerDirective } from './type-checker.directive';

describe('TypeCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new TypeCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
